        <!-- Navbar START -->
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container-fluid">
                <div class="navbar-header">
                    <ul class="nav navbar-nav navbar-left">
                        <li><a class="navbar-brand" href="/index"><img class="brand-img" alt="Brand" src="/assets/img/scraper-green.png"></a></li>
                        <li>
                            <div class="xtooltip">
                                <a class="navbar-brand" href="/index"><h4>{{heading}}</h4></a>
                                <span class="xtooltiptext" style="bottom: -75%;">Reload this page.</span>
                            </div>
                        </li>
                    </ul>
                </div>
                <div class="navbar-header">
                    <ul class="nav navbar-nav">
                        <li><a class="active navbar-brand" href="{{origin}}" target="_blank"><h4>Scraping from : {{origin}}</h4></a></li>
                    </ul>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <li><form class="navbar-form navbar-right" method="post" target="_self" action="/api/scrape">
                            <div class="xtooltip">
                                <button type="submit" class="btn btn-link navbar-brand">New Scrape</button>
                                <span class="xtooltiptext" style="bottom: -75%;">Initiate a scrape to look for new items.</span>
                            </div>
                            <div class="form-group hidden-input">
                            {{#each issues}}
                                <input id="issueID{{@index}}" name="issueID{{@index}}" type="hidden" value="{{this._id}}">
                                <input id="issueNAME{{@index}}" name="issueNAME{{@index}}" type="hidden" value="{{this.issue}}">
                            {{/each}}
                            </div>
                        </form></li>
                    </ul>
                </div>
            </div>
        </nav>
        <!-- Navbar END -->
        <!-- Opening Paragraph START -->
        <div class="row">
            <div class="col-md-1 col-sm-3"></div>
            <div class="col-md-10 col-sm-6 col-xs-12">
                <p class="h4">Welcome to the "IoT Weekly News" scraper. This application was developed as a homework assignment to demonstrate -
                    <ul>
                        <li><b>Node - Express :</b> Creates a server that responds to specific HTTP methods and paths.</li>
                        <li><b>Mongoose - MongoDB :</b> Using Mongoose to create the schemas and provide a means to relate data in different collections.</li>
                        <li><b>Request - Cheerio :</b> Request a web page from a "news" site and use Cheerio to navivate the DOM and extract data.</li>
                        <li><b>Body Parser - Handlebars :</b> Form POST data handling, and page rendering.</li>
                    </ul>
                </p>
            </div>
            <div class="col-md-1 col-sm-3"></div>
        </div>
        <!-- Opening Paragraph END -->
        <br><br>
        <!-- Issue Selector Form START -->
        <form method="post" target="_self" action="/api/list">
            <!-- Issue List START -->
            <div class="row center">
                <div class="col-md-3 col-sm-3"></div>
                <div class="col-md-6 col-sm-6 col-xs-12">
                    <select id="issueselect" name="issueselect" type="text" class="ui fluid dropdown" required>
                        <option value="">Choose an Issue</option>
                        {{#each issues}}
                            <option value={{this._id}}>{{this.issue}}</option>
                        {{/each}}
                    </select>
                </div>
                <div class="col-md-3 col-sm-3"></div>
            </div>
            <div class="row center">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <br>
                    {{{scrapemessage}}}
                    <br>
                    <br>
                </div>
            </div>
            <div class="row center">
                <div class="col-md-3 col-sm-3"> </div>
                <div class="col-md-6 col-sm-6 col-xs-12">
                    <div class="xtooltip">
                        <button type="submit" class="btn btn-link">List Scraped Items</button>
                        <span class="xtooltiptext">List all of the items in the selected Issue.</span>
                    <div>
                </div>
                <div class="col-md-3 col-sm-3"></div>
            </div>
           <!-- Issue List END -->
        </form>
        <!-- Issue Selector Form END -->
        <div class="row center">
            <div class="col-md-12 col-sm-12 col-xs-12"><br></div>
        </div>
